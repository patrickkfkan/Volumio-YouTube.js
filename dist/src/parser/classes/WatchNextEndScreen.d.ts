import { YTNode } from '../helpers';
declare class WatchNextEndScreen extends YTNode {
    static type: string;
    results: import("../helpers").SuperParsedResult<YTNode>;
    title: string;
    constructor(data: any);
}
export default WatchNextEndScreen;
